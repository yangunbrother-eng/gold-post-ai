"use client";

// PC·모바일 단일 UI: 사이드바 미사용 (상단바로 통일)
// 기존 import 유지용 더미 (각 페이지에서 <Sidebar .../> 호출 그대로 둠)
export default function Sidebar({ bizName, extConnected }: { bizName: string; extConnected: boolean }) {
  void bizName;
  void extConnected;
  return null;
}
